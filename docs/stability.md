# 稳定性优化记录

## 目标

在主流程中加入基础异常处理，避免程序在常见错误情况下直接崩溃。

## 已处理情况

- 输入音频文件不存在；
- 本地 GGUF 模型文件不存在；
- ASR 没有识别到有效文本；
- LLM 没有生成有效回复；
- TTS 生成失败；
- 录音模式在 WSL 环境下不可用。

## 当前策略

对于音频文件和模型文件，程序会在运行前检查路径是否存在。

对于 ASR、LLM、TTS 主流程，使用 try-except 捕获异常，并输出错误信息。


